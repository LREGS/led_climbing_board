from setuptools import setup, find_packages

setup(
    name="ledBoard",
    version=0.1,
    packages=find_packages(),
    install_requires=[],
    author="William",
    author_email="github.com/lregs"


)